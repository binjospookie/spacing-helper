
name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: 14.15.3-alpine3.10

    steps:
      - uses: actions/checkout@v2
      - run: yarn
      - run: yarn run build
      - run: node ../../src/__tests__/index.spec.ts --coverage
